### main()
- Presentation層へユーザーIO系の処理を移動
- 関数名、変数名、引数名などを意味が伝わりやすいよう改善
- コマンドに紐づくそれぞれのロジックをメソッド単位に切り分け
- 変数を含む出力にf-stringを使用するように変更

### a()
- Infrastructure層へロジックを移動
- csvモジュールを使用するように変更
- 関数名、変数名、引数名などを意味が伝わりやすいよう改善
- 使い終わったストリームを閉じるようにwith文を追加
- 引数、戻り値 (None) に型を追加

### f()
- Infrastructure層へロジックを移動
- csvモジュールを使用するように変更
- 関数名、変数名、引数名などを意味が伝わりやすいよう改善
- 使い終わったストリームを閉じるようにwith文を追加
- 引数、戻り値に型を追加
- exceptに特定の例外 (`FileNotFoundError`) を指定するように変更

### やり残し
- Bookデータの削除ロジック追加
- BookRepositoryインターフェースを作って、CsvBookRepositoryを外から渡すように変更
- テストコード追加
- 価格をintにして、入力に対してバリデーションの追加
- CSVファイルのパスを外から渡すように変更
